package main

import "fmt"

func main() {

	var str string = "hello"
	fmt.Println(str+" oh")
	fmt.Println(str[0])
	// We can't manuplate one char 
	// but we can change whole sentace

	str ="hey what's up"
	fmt.Println(str[2:10])


	
}